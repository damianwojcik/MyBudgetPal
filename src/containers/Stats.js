import React from 'react';
import BottomBar from '../Components/BottomBar/BottomBar';

const Stats = () => {
    return (
        <div>
            <h1>Stats</h1>
            <BottomBar />
        </div>
    );
};

export default Stats;